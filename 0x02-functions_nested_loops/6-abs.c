#include "holberton.h"
/**
  * _abs - Function that computes the absolutes value of an integer.
  * Return: Always 0.
  * @i: Number to check.
  */

int _abs(int i)
{
	if (i < 0)
	i = i * -1;

	else
	i = i;


return (i);

}
