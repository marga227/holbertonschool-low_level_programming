#include "holberton.h"
/**
  * swap_int - Swaps the values of two integers.
  * Return: Nothing.
  * @a: change to b.
  * @b: change to a.
  */

void swap_int(int *a, int *b)
{
	int temp = *a;
	*a = *b;
	*b = temp;



}
