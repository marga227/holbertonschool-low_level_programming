#include <stdio.h>
/* C program to print all alphabets from a to z */
/**
 *main -Prints the alphabet.
 *
 *Return: Alwais 0 (Sucess)
 */
int main(void)
{
	char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
	putchar (ch);
}
	putchar ('\n');
return (0);

}
