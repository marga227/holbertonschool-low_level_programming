#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
/**
 *main -Programa que imprime  el alphabet in lowercase and then uppercase.
 *
 *Return: Alwais 0 (Sucess)
 */
int main(void)
{
	int ch;
	int 
for (ch = 'a' ; ch <= 'z' ; ch++)
{
	putchar (ch);

	for (ch = 'A' ; ch <= 'Z' ; ch++)
	
	putchar (ch)
}
return (0);
}
